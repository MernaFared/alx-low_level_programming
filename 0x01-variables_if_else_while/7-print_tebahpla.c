#include <stdio.h>
/**
* main -print if the number is postive, zero, or negative
* Return: 0
*
*/
int main(void)
{
char abc;
for (abc = 'z'; abc >= 'a'; abc--)
{
putchar(abc);
}
putchar('\n');
return (0);
}
